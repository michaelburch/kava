<Window
    x:Class="Kava.Windows.FlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Kava">

    <Grid RequestedTheme="Dark"
          Padding="0">
        <Grid.Background>
            <SolidColorBrush Color="#1E1E1E"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Date Strip -->
        <ScrollViewer Grid.Row="0"
                      HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollBarVisibility="Disabled"
                      HorizontalScrollMode="Enabled"
                      Padding="12,16,12,4">
            <StackPanel x:Name="DateStripPanel" Orientation="Horizontal" Spacing="0"/>
        </ScrollViewer>

        <!-- Date Heading + Add button -->
        <Grid Grid.Row="1" Padding="16,8,16,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="DateHeading"
                       Text="March 28"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="White"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Padding="8"
                    ToolTipService.ToolTip="New event">
                <FontIcon Glyph="&#xE710;" FontSize="14" Foreground="#999999"/>
            </Button>
        </Grid>

        <!-- Agenda Event List -->
        <ScrollViewer Grid.Row="2" Padding="8,0,8,8">
            <StackPanel x:Name="EventListPanel" Spacing="2">
                <!-- Empty state -->
                <TextBlock x:Name="EmptyState"
                           Text="No events for this day"
                           FontSize="13"
                           Foreground="#888888"
                           HorizontalAlignment="Center"
                           Margin="0,40,0,0"
                           Visibility="Collapsed"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
